#制定eureka服务的地址
eureka:
  client:
    service-url:
        defaultZone: http://root:root@localhost:8761/eureka,http://root:root@localhost:8762/eureka


# 制定服务名称
spring:
  application:
    name: SEARCH

#端口号
server:
  port: 8081
